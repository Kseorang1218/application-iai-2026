[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "thesis-server"
version = "0.1.0"

[tool.setuptools]
packages = ["funs", "models"]

[tool.setuptools.package-dir]
"funs" = "funs"
"models" = "models"
